// TODO: migrate real implementation from akhiakl/svgin-react. Must match its
// current published feature set (export paths /client, /server, /core,
// /suspense, /shadow, /all; sanitization guarantees) at minimum, see the
// svgin-monorepo skill's migration-fidelity note.
export const placeholder = 'svgin-react' as const;
