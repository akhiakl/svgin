export default function Home() {
    return (
        <main>
            <h1>svgin: try it</h1>
            {/* TODO: demo svgin-react and svgin-element here once they have
                real implementations. Keep this app in sync with every
                svgin-react/svgin-element release, see the svgin-monorepo
                skill. */}
        </main>
    );
}
